query Launch($launchId: ID!) {
    launch(id: $launchId) {
        id
        details
        launch_year
        launch_site {
            site_name
        }
        links {
            mission_patch
        }
        mission_name
    }
}